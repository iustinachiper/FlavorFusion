<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FlavorFusion.MealPlans"
             Title="Meal Plans">
    <VerticalStackLayout Padding="20" Spacing="20">
        <!-- Buton pentru adăugare plan -->
        <Button Text="Create New Meal Plan"
                BackgroundColor="#4CAF50"
                TextColor="White"
                CornerRadius="10"
                HorizontalOptions="Start"
                Command="{Binding NavigateToCreateMealPlanCommand}" />

        <!-- Tabel cu planuri -->
        <Grid ColumnDefinitions="2*, 3*, 1*" RowSpacing="10">
            <!-- Header -->
            <Label Text="Meal Plan Name" FontAttributes="Bold" FontSize="16" Grid.Column="0" />
            <Label Text="Description" FontAttributes="Bold" FontSize="16" Grid.Column="1" />
            <Label Text="Actions" FontAttributes="Bold" FontSize="16" Grid.Column="2" HorizontalOptions="End" />

            <!-- Listează planurile -->
            <CollectionView Grid.Row="1" Grid.ColumnSpan="3" ItemsSource="{Binding MealPlans}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="2*, 3*, 1*" RowSpacing="5">
                            <!-- Numele planului -->
                            <Label Text="{Binding Name}" VerticalOptions="Center" Grid.Column="0" />

                            <!-- Descrierea planului -->
                            <Label Text="{Binding Description}" VerticalOptions="Center" Grid.Column="1" />

                            <!-- Acțiuni -->
                            <StackLayout Orientation="Horizontal" HorizontalOptions="End" Grid.Column="2">
                                <Button Text="Edit"
                                        BackgroundColor="LightGray"
                                        TextColor="Black"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditMealPlanCommand}"
                                        CommandParameter="{Binding}" />
                                <Button Text="Delete"
                                        BackgroundColor="Red"
                                        TextColor="White"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteMealPlanCommand}"
                                        CommandParameter="{Binding}" />
                            </StackLayout>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </VerticalStackLayout>
</ContentPage>